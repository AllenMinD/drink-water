import React, { memo } from 'react';

//私有常量

//可抽离的逻辑处理函数/组件

let HomePage = (props: IProps) => {
    //变量声明、解构

    //组件状态

    //网络IO

    //数据转换

    //逻辑处理函数

    //组件Effect

    return (
        <div>首页</div>
    );
};

//props类型定义
interface IProps {
    
}

//prop-type定义，可选

export default memo(HomePage);
