import React, { memo } from 'react';
import UserSetting from '../Components/UserSetting';

//私有常量

//可抽离的逻辑处理函数/组件

let UserSettingPage = (props: IProps) => {
    //变量声明、解构

    //组件状态

    //网络IO

    //数据转换

    //逻辑处理函数

    //组件Effect

    return (
        <div>
            <UserSetting />
        </div>
    )
};

//props类型定义
interface IProps {
    
}

//prop-type定义，可选
export default memo(UserSettingPage);
