import React from 'react';
import './index.less';

//私有常量

//可抽离的逻辑处理函数/组件

let UserSetting = (_props: IProps) => {
    //变量声明、解构

    //组件状态

    //网络IO

    //数据转换

    //逻辑处理函数

    //组件Effect

    return (
        <div styleName="container">hello</div>
    )
};

//props类型定义
interface IProps {
    
}

//prop-type定义，可选

export default UserSetting;